import { ToolPageLayout } from "@/components/tools/ToolPageLayout"
import { TOOLS } from "@/lib/toolConfigs"

export default function RestorePointManagerPage() {
  return <ToolPageLayout tool={TOOLS["restore-point-manager"]} />
}
