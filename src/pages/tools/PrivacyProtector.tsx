import { ToolPageLayout } from "@/components/tools/ToolPageLayout"
import { TOOLS } from "@/lib/toolConfigs"

export default function PrivacyProtectorPage() {
  return <ToolPageLayout tool={TOOLS["privacy-protector"]} />
}