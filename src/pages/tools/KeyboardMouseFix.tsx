import { ToolPageLayout } from "@/components/tools/ToolPageLayout"
import { TOOLS } from "@/lib/toolConfigs"

export default function KeyboardMouseFixPage() {
  return <ToolPageLayout tool={TOOLS["keyboard-mouse-fix"]} />
}
