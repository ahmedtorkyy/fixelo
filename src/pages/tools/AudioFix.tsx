import { ToolPageLayout } from "@/components/tools/ToolPageLayout"
import { TOOLS } from "@/lib/toolConfigs"

export default function AudioFixPage() {
  return <ToolPageLayout tool={TOOLS["audio-fix"]} />
}