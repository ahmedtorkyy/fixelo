import { ToolPageLayout } from "@/components/tools/ToolPageLayout"
import { TOOLS } from "@/lib/toolConfigs"

export default function DisplayResolutionFixPage() {
  return <ToolPageLayout tool={TOOLS["display-resolution-fix"]} />
}