import { ToolPageLayout } from "@/components/tools/ToolPageLayout"
import { TOOLS } from "@/lib/toolConfigs"

export default function DateTimeFormatFixPage() {
  return <ToolPageLayout tool={TOOLS["date-time-format-fix"]} />
}
