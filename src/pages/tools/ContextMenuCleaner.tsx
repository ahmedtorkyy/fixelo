import { ToolPageLayout } from "@/components/tools/ToolPageLayout"
import { TOOLS } from "@/lib/toolConfigs"

export default function ContextMenuCleanerPage() {
  return <ToolPageLayout tool={TOOLS["context-menu-cleaner"]} />
}
